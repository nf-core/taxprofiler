# yaml-language-server: $schema=https://raw.githubusercontent.com/nf-core/modules/master/subworkflows/yaml-schema.json
name: shortread_adapterremoval
description: Remove adapters from (Illumina) short-read sequencing data with AdapterRemoval
keywords:
  - adapter removal
  - preprocessing
  - short reads
components:
  - adapterremoval
  - cat/fastq
input:
  - ch_reads:
      type: file
      description: |
        Short-read sequencing reads from typically Illumina sequencers in FASTQ format.
        Structure: [ val(meta), path(reads) ]
      pattern: "*.{fastq,fastq.gz,fq,fq.gz}"
  - ch_adapterlist:
      type: file
      description: |
        Optional file containing list of adapters to remove.
        Structure: [ val(meta), path(custom_adapters) ]
      pattern: "*"
output:
  - reads:
      type: file
      description: |
        Adapter removed and processed reads
        Structure: [ val(meta), path(reads) ]
      pattern: "*.fastq.gz"
  - mqc:
      type: file
      description: |
        Log files for MultiQC
        Structure: [ path(files) ]
      pattern: "*"
  - versions:
      type: file
      description: |
        File containing software versions
        Structure: path(versions.yml)
      pattern: "versions.yml"
authors:
  - "@jfy133"
maintainers:
  - "@jfy133"
