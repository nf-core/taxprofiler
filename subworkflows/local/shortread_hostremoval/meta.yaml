# yaml-language-server: $schema=https://raw.githubusercontent.com/nf-core/modules/master/subworkflows/yaml-schema.json
name: xx
description: xx
keywords:
  - xx
  - xx
  - xx
components:
  - xx/xx
input:
  - ch_xx:
      type: xx
      description: |
        x
        Structure: [ val(xx), path(x) ]
output:
  - xx:
      type: x
      description: |
        xx
        Structure: [ val(xx), path(xx) ]
      pattern: "*.xx"
  - mqc:
      type: file
      description: |
        Log files for MultiQC
        Structure: [ path(files) ]
      pattern: "*"
  - versions:
      type: x
      description: |
        File containing software versions
        Structure: path(versions.yml)
      pattern: "versions.yml"
authors:
  - "@xx"
maintainers:
  - "@xx"
